To run the backend server in a separate terminal, please execute the following commands:

First, navigate to the backend directory:
cd backend

Then, run the uvicorn server:
uvicorn src.main:app --host 0.0.0.0 --port 8000
